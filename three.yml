---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

    - name: "just execute a ls -lrtg command"
      shell: "ls -lrtg"
      register: "output"

    - debug: var=output.stdout_lines
